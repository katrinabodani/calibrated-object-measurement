# Models

Training configs and trained weights.

## Contents
- `configs/` — training configuration files (tracked in git)
- `weights/` — trained model checkpoints (**NOT in git** — hosted on Drive)

See `docs/TRAINING_REPORT.md` for architecture, hyperparameters, and metrics.

## 📂 Large files — Google Drive
> Per Section 2.2, trained model weights are hosted externally.

**Trained Model Weights:** _`<PASTE DRIVE URL>`_

_Link access must be set to "Anyone with the link can view."_
