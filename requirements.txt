# Core computer vision & numerics
opencv-python>=4.8.0
numpy>=1.24.0
scipy>=1.10.0

# Imaging / plotting
matplotlib>=3.7.0
Pillow>=10.0.0
pillow-heif>=0.15.0  

# Model training: Mask R-CNN (torchvision, ResNet50-FPN).
# Install torch/torchvision with the CUDA build matching your GPU, e.g.:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
torch>=2.6.0
torchvision>=0.21.0
pycocotools>=2.0.7          # COCO mAP evaluation + RLE mask decode

# Utilities
tqdm>=4.65.0
pyyaml>=6.0
pandas>=2.0.0